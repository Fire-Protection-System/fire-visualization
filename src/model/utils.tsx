import { FileSystemNode } from './FileSystemNode';
import { NodeTypeEnum } from './NodeTypeEnum';

export const getSampleFileSystem = (): FileSystemNode[] => {
  return []; // Placeholder for sample file system data
};
